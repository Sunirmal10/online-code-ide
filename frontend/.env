# API url needs to be prefixed with VITE_ to be exposed to your client-side code in vite apps
# on backend, the primary port is 3000 with the alternate port being 5000, which is mentioned in .env.alt in the frontend folder
VITE_API_URL="http://localhost:3000"